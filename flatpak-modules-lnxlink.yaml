# Generated with flatpak-pip-generator --yaml --output /tmp/flatpak-modules-lnxlink --ignore-errors --requirements-file /tmp/requirements_pinned.txt
name: flatpak-modules-lnxlink
buildsystem: simple
build-commands: []
modules:
  - name: python3-lnxlink
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "." --no-build-isolation
    sources: []
  - name: python3-PyYAML
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "PyYAML>=6.0.1" --no-build-isolation
    sources: []
  - name: python3-aiohttp
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "aiohttp>=3.9.0" --no-build-isolation
    sources: []
  - name: python3-beaupy
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "beaupy>=3.10.0" --no-build-isolation
    sources: []
  - name: python3-distro
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "distro>=1.9.0" --no-build-isolation
    sources: []
  - name: python3-inotify
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "inotify>=0.2.10" --no-build-isolation
    sources: []
  - name: python3-jeepney
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "jeepney>=0.9.0" --no-build-isolation
    sources: []
  - name: python3-paho-mqtt
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "paho-mqtt>=2.1.0" --no-build-isolation
    sources: []
  - name: python3-psutil
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "psutil>=7.0.0" --no-build-isolation
    sources: []
  - name: python3-requests
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "requests>=2.31.0" --no-build-isolation
    sources: []
  - name: python3-PyGObject
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "PyGObject>=3.44.0" --no-build-isolation
    sources: []
  - name: python3-SpeechRecognition
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "SpeechRecognition>=3.10.0" --no-build-isolation
    sources: []
  - name: python3-dbus-idle
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "dbus-idle>=2026.8.0" --no-build-isolation
    sources: []
  - name: python3-dbus-mediaplayer
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "dbus-mediaplayer>=2026.7.0" --no-build-isolation
    sources: []
  - name: python3-dbus-networkdevices
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "dbus-networkdevices>=2026.7.0" --no-build-isolation
    sources: []
  - name: python3-dbus-notification
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "dbus-notification>=2026.7.0" --no-build-isolation
    sources: []
  - name: python3-docker
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "docker>=7.0.0" --no-build-isolation
    sources: []
  - name: python3-ewmh
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "ewmh>=0.1.6" --no-build-isolation
    sources: []
  - name: python3-flask
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "flask>=3.0.3" --no-build-isolation
    sources: []
  - name: python3-nvitop
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "nvitop>=1.3.2" --no-build-isolation
    sources: []
  - name: python3-nvsmi
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "nvsmi>=0.4.2" --no-build-isolation
    sources: []
  - name: python3-opencv-python
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "opencv-python>=4.7.0.68" --no-build-isolation
    sources: []
  - name: python3-pulsectl
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "pulsectl>=23.5.2" --no-build-isolation
    sources: []
  - name: python3-pyalsaaudio
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "pyalsaaudio>=0.9.2" --no-build-isolation
    sources: []
  - name: python3-pyamdgpuinfo
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "pyamdgpuinfo>=2.1.4" --no-build-isolation
    sources: []
  - name: python3-python-xlib
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "python-xlib>=0.33" --no-build-isolation
    sources: []
  - name: python3-vdf
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "vdf>=3.4" --no-build-isolation
    sources: []
  - name: python3-waitress
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "waitress>=3.0.0" --no-build-isolation
    sources: []
  - name: python3-xlib-hotkeys
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} "xlib-hotkeys>=2024.3.0" --no-build-isolation
    sources: []
